import { initTheme } from './theme/theme-ui.js';
import { applyTheme, applyHeaderToggleVisibility } from './theme/theme-engine.js';

/**
 * Facade for Theme Feature
 */

export { initTheme, applyTheme, applyHeaderToggleVisibility };

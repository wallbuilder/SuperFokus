Here is a summary of the restructuring actions performed specifically concerning the `src/main` folder and its files:

1. **Directory Creation**: Created the `src/main` directory to house all Electron main process files.
2. **Moved Core Files**: 
   - Moved `main.js` (the main Electron entry point) to `src/main/main.js`.
   - Moved `preload.js` (the IPC bridge) to `src/main/preload.js`.
3. **Moved Helper & Utility Files**:
   - Moved `fokus-sb-helper.js` (the Site Blocker helper script) to `src/main/fokus-sb-helper.js`.
   - Moved `utils.js` (shared utilities for the main process) to `src/main/utils.js`.
4. **Updated Import Paths in `main.js`**:
   - Updated the `require` path for `utils.js` to correctly resolve locally within the `src/main` folder (`require('./utils')`).
   - Updated the `fokus-sb-helper.js` execution path reference.
   - Updated window icon paths to point to `../../assets/fokusicon.png`.
   - Updated `loadFile` paths for the various application windows to reflect their new locations in the modular structure:
     - Popup Window: `src/renderer/ui/popup.html`
     - Pomo Timer Window: `src/renderer/features/pomo-timer.html`
     - Fullscreen Popup: `src/renderer/ui/fullscreen-popup.html`
5. **Updated Import Paths in `preload.js`**:
   - Updated the `require` path for `utils.js` to correctly resolve locally (`require('./utils')`).
6. **Package Initialization**:
   - Updated `package.json` to point the `"main"` entry point to the new `src/main/main.js` location so the app boots correctly.

7. **Flow State Mode Enhancements**:
   - Added a seconds field to the Flow State Stopwatch configuration for more precise chime intervals.
   - Implemented a dedicated Flow State popup window (`src/renderer/features/flow-timer.html`) that displays a real-time stopwatch.
   - Updated `src/main/main.js` and `src/main/preload.js` with new IPC channels to manage the flow timer lifecycle.

8. **UI/UX Consistency Refinement**:
   - Redesigned the Repeating Reminders popup to use the centralized `src/renderer/ui/popup.html` system.
   - The popup now features a modern, themed design with a dynamic auto-close countdown button, consistent with the rest of the application's aesthetic.
   - Integrated theme synchronization so all popups instantly reflect dark/light mode changes from the main dashboard.

9. **Micro-Task Sprint Enhancements**:
   - Added an "Autostart next task" option to the configuration using a modern-styled toggle switch.
   - Implemented a dedicated Micro-Task Sprint timer popup window (`src/renderer/features/micro-sprint-timer.html`) modeled after the Pomo Style timer.
   - Updated `src/main/main.js` and `src/main/preload.js` with new IPC channels (`open-micro-sprint-timer`, `update-micro-sprint-timer`, `close-micro-sprint-timer`) to manage the popup lifecycle.
   - Enhanced the preset system to include the autostart preference and provided the ability to delete custom sprint presets.
   - Refined the task transition logic to support automatic progression with a 2-second delay between tasks when autostart is active.
